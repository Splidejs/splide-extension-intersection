import { IntersectionOptions } from '../types/options';


/**
 * Default options for the Intersection extension.
 *
 * @since 0.1.0
 */
export const DEFAULTS: IntersectionOptions = {
  // root: null,
};